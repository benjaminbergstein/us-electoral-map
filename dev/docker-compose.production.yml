version: '3.7'

services:
    app:
        image: benbergstein/us-electoral-map:latest
        networks:
          - default
          - nginx-proxy

networks:
  default:
  nginx-proxy:
    external: true
